class Solution {
    public int reverse(int x) {
        if (x > Integer.MAX_VALUE || x < Integer.Min_Value)
            return 0;
        
        boolean negative = false;
        if (x < 0)
            negative = true;
        
        
        
        
    }
}